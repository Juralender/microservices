apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: hw23

resources:
  - namespace.yaml
  - secrets.yaml
  - keycloak.yaml
  - records-db.yaml
  - records-service.yaml
  - traefik.yaml

configMapGenerator:
  - name: keycloak-realm-config
    files:
      - keycloak-config/hw23-realm.json
    options:
      labels:
        app: keycloak
  - name: traefik-static-config
    files:
      - traefik-config/traefik.yml
    options:
      labels:
        app: traefik
  - name: traefik-dynamic-config
    files:
      - traefik-config/dynamic.yml
    options:
      labels:
        app: traefik
